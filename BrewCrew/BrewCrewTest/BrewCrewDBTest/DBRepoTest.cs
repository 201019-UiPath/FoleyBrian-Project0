using Xunit;
using Microsoft.EntityFrameworkCore.InMemory;
using BrewCrewDB;
using BrewCrewDB.Models;

namespace BrewCrewTest.BrewCrewDBTest
{
    public class DBRepoTest
    {
        //private readonly IMapper mapper = new DBMapper();

        // private DBRepoTest repo;

        // private readonly testCustomer = new Customer()
        // {
        //     // initialize customer object properties here

            
        // };

        // private readonly testManager = new Manager()
        // {
        //     // init Manager object here
        // };

        // private readonly List<Users> usertypes = new List<Users>()
        // {
        //     new User() {Chartype = "Superhero"},
        //     new User() {Chartype = "Supervillain"}
        // };

        // // add data to db here using context

        // [Fact]
        // //implement tests
        // public void AddManagerShouldAdd()
        // {
        //     //Arrange
        //     var options = new DbContextBuilder<HeroContext>().UseInMemoryDatabase("AddHeroesShouldAdd").Options;
        //     using var testContext = new BrewCrewContext(options);
        //     repo = new DBRepo(testContext,mapper);
        //     Seed(testContext);

        //     //Act
        //     repo.AddManagerAsync(testManager);

        //     //assert
        //     using var assertContext = new HeroContext()
        //     Assert.NotNull(assertContext.Users.Single(u => u.name == testManager.Alias));

        //     //run "dotnet test" in terminal
        // }
    }
}